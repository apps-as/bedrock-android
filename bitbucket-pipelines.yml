pipelines:
  default:
    - step:
        name: Commit work
        script:
          - echo "Thank you for feeding me with more code!"
    - step:
        caches:
          - gradle
        name: Publish to Maven
        image: androidsdk/android-30
        trigger: manual
        deployment: production
        script:
          - bash ./gradlew :bedrock:publish -Pmaven_access=$maven_access -Pmaven_secret=$maven_secret
